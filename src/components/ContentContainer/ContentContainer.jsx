import { ContentContainerStyled } from "./ContentContainer.styled.jsx";

const ContentContainer = ({ children }) => {
  return <ContentContainerStyled>{children}</ContentContainerStyled>;
};

export default ContentContainer;
